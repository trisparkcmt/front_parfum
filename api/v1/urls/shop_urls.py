from django.urls import path

# Imports des views (à remplir quand tu créeras les views)
# from catalogue.views import ...

urlpatterns = [
    # Catégories de parfums
    # path('categories/', CategorieParfumListView.as_view(), name='categories-list'),

    # Parfums
    # path('parfums/', ParfumListView.as_view(), name='parfums-list'),
    # path('parfums/<slug:slug>/', ParfumDetailView.as_view(), name='parfum-detail'),

    # Essences
    # path('essences/', EssenceListView.as_view(), name='essences-list'),

    # Accessoires
    # path('accessoires/', AccessoireListView.as_view(), name='accessoires-list'),
    # path('accessoires/<slug:slug>/', AccessoireDetailView.as_view(), name='accessoire-detail'),

    # Flacons
    # path('flacons/', FlaconListView.as_view(), name='flacons-list'),

    # Tags
    # path('tags/', TagListView.as_view(), name='tags-list'),
]
